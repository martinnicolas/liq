<%= form_for(@concepto) do |f| %>
  <% if @concepto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concepto.errors.count, "error") %> prohibited this concepto from being saved:</h2>

      <ul>
      <% @concepto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class="formulario">
    <tr><th><%= f.label :codigo, "Código" %></th><td><%= f.text_field :codigo, :style => "width: 100px;" %></td></tr>
    <tr><th><%= f.label :nombre %></th><td><%= f.text_field :nombre, :style => "width: 300px;" %></td></tr>
    <tr><th><%= f.label :calculo_cargos, "Cálculo de cargos" %></th><td><%= f.text_area :calculo_cargos, :cols => 40, :rows => 5 %></td></tr>
    <tr><th><%= f.label :calculo_horas_media, "Cálculo de horas de media" %></th><td><%= f.text_area :calculo_horas_media, :cols => 40, :rows => 5 %></td></tr>
    <tr><th><%= f.label :calculo_horas_superior, "Cálculo de horas de nivel superior" %></th><td><%= f.text_area :calculo_horas_superior, :cols => 40, :rows => 5 %></td></tr>
    <tr><th><%= f.label :requerido %></th><td><%= f.text_field :requerido, :style => "width: 100px;" %></td></tr>
    <tr><th><%= f.label :carga_manual %></th><td><%= f.text_field :carga_manual, :style => "width: 100px;" %></td></tr>
    <tr><th><%= f.label :ayuda %></th><td><%= f.text_area :ayuda, :cols => 40, :rows => 5 %></td></tr>
    <tr><th><%= f.label :activado_x_defecto, "Activado por defecto" %></th><td><%= f.text_field :activado_x_defecto, :style => "width: 100px;" %></td></tr>
    <tr><th><%= f.label :tipo %></th><td><%= f.text_field :tipo, :style => "width: 150px;" %></td></tr>
    <tr><th><%= f.label :tipo_liquidacion %></th><td><%= f.text_field :tipo_liquidacion, :style => "width: 150px;" %></td></tr>
    </table> 

  <div class="actions">
    <%= f.submit :class => "btn btn-primary", :value => 'Guardar' %>
  </div>  

<% end %>
